@using LaweyrServices.Web.Shared.LawyerViewModels

<RadzenCard class="m-3">
                <h3 class="h5">Contact</h3>
                <div class="d-flex flex-row">
                    <RadzenImage Path="@Lawyer.ImgUrl" Class="rounded-circle float-left mr-3" Style="width: 100px; height: 100px;" />
                    <div>
                      @*  <div>@(Lawyer.FirstName + " " + Lawyer.LastName)</div>
                        <b>@(order.Employee?.FirstName + " " + order.Employee?.LastName)</b>
                        <div class="mt-3">@Lawyer.</div>
                        <b>@order.Customer?.CompanyName</b>*@
                    </div>
                </div>
            </RadzenCard>
@code {

    [Parameter]
    public LawyerListItem Lawyer { get; set; }
}
