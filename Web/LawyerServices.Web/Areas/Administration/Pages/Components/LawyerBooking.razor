@page  "/lawyerbooking"
@using LawyerServices.Common.WorkingTimeModels
@using LawyerServices.Services.Data
@inject NavigationManager navigationManager
@inject IWorkingTimeExceptionService wteService

@layout Administration.Shared.AdminLayout
<AuthorizeView Roles="Lawyer">
    <Authorized>
        <button type="button" class="btn btn-primary">
            Заявки <span class="badge badge-light"></span>
        </button>
        <div class="container-fluid p-0">
            <div class="row no-gutters mx-n2">
                <div class="col-sm-12 my-5">
                   
                </div>

            </div>

        </div>
    </Authorized>
    <NotAuthorized>
      @*  <h1>Authentication Failure!</h1>
        <p>You're not authorize.</p>*@
        @{
            navigationManager.NavigateTo("/Profile");
        }
    </NotAuthorized>
</AuthorizeView>>

@code {

    WorkingTimeExceptionBookingModel model;

    protected override void OnInitialized()
    {
        base.OnInitialized();
        model = this.wteService.AcceptRequest("a10e3666-a7d2-451f-a5be-f6dd03cb282a").Result;
    }

}
